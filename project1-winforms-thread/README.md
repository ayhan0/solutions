# Project 1 - WinForms Prime Calculator (Thread)

- Two independent calculations run concurrently using `Thread`.
- No `Task` / async-await is used.
- UI updates are done via `BeginInvoke` to avoid cross-thread UI access.
- UI is created programmatically (no Designer).
